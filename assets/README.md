Drop your portrait here as `portrait.jpg` (or update the path in `index.html`).
A portrait-orientation photo around 4:5 aspect works best, since it's framed by CSS in `style.css` under `.portrait img`.
